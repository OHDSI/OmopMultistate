name: Weekly tests

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * 1"

permissions:
  contents: read

jobs:
  duckdb-CDMConnector:
    uses: ./.github/workflows/test-template.yaml
    with:
      DB_TO_TEST: duckdb-CDMConnector

  local-omopgenerics:
    uses: ./.github/workflows/test-template.yaml
    with:
      DB_TO_TEST: local-omopgenerics
